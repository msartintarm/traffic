{
  "name": "traffic-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "wasm:build": "cd ../engine && wasm-pack build --target web --out-dir ../web/public/wasm-pkg --out-name engine",
    "predev": "npm run wasm:build",
    "dev": "next dev -p 3000",
    "prebuild": "npm run wasm:build",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
